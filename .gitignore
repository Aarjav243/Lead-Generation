# Python virtualenv
.venv/
venv/
__pycache__/
*.pyc

# Reticle dev-only bundle (regenerate: npx esbuild reticle-dev-entry.js --bundle --format=esm --outfile=public/_reticle-dev.js)
webapp/public/_reticle-dev.js
.reticle/

# Cloudflare local dev state — holds a local D1 copy that can contain real scraped leads
webapp/.wrangler/

# Claude Code agent worktrees — local tooling scratch, not project content
.claude/worktrees/

# Node
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
